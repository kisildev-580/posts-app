/**
 * @notes
 * this can be updated as you go
 * (it's just to get started 👌)
 */

export type PostType = {
    userId: number;
    id: number;
    title: string;
    body: string;
  };

  export type AuthorType = {
    name: string;
    email: string;
  };
  